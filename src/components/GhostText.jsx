import React from 'react'

export const GhostText = ({ text, position }) => {
    return (
        <div
            className="text-gray-400">

                
            {text}
        </div>
    )
}